import { Subjects } from './subjects';
export interface TicketUpdatEvent {
    subject: Subjects.TicketUpdated;
    data: {
        id: string;
        title: string;
        price: number;
        userId: string;
    };
}
